module('Base test setup');

test('This should get things started', function () {
    ok(1 == '1', 'Hurray!');
});

test('This should get the grunt watch started', function () {
    ok(2 == '2', 'Hurray!');
});
